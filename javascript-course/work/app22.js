

// Document Object Model.
// Think of it as a structure, with a bunch of nodes.
// Document links to html-> links to either body or head -> links to either a element, h1 element, etc. 
// DOM is object oriented, so each node has its own set of properties that we can add or remove.
// browser gives us a window object, inside there we have document object.
// we have root element underneat that, which is the html element.
// from there we have body tags, head tags, which are siblings.
// in the head, we have meta tags, title...
// in the body, we have h1 tags, links, html 5 header semantic tags, things like that.
// if we're not familliar with javascript, but we've built many elements with HTML, so we're familiar, 
//      we can use javascript to manipulate elements.
// Can use jquery to manipulate elemenets, but now we can use vanilla JS, and we'll try to do that. 
